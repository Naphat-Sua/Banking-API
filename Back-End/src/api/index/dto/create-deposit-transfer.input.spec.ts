import { CreateDepositTransferInput } from './create-deposit-transfer.input';

describe('CreateDepositTransferInput', () => {
  it('should be defined', () => {
    expect(new CreateDepositTransferInput()).toBeDefined();
  });
});
