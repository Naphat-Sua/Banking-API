import { QueryDepositOutput } from './query-deposit.output';

describe('QueryDepositOutput', () => {
  it('should be defined', () => {
    expect(new QueryDepositOutput()).toBeDefined();
  });
});
