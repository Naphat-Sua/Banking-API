import { QueryWithdrawInput } from './query-withdraw.input';

describe('QueryWithdrawInput', () => {
  it('should be defined', () => {
    expect(new QueryWithdrawInput()).toBeDefined();
  });
});
