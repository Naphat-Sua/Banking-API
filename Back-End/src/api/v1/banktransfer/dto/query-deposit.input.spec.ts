import { QueryDepositInput } from './query-deposit.input';

describe('QueryDepositInput', () => {
  it('should be defined', () => {
    expect(new QueryDepositInput()).toBeDefined();
  });
});
