import { QueryWithdrawOutput } from './query-withdraw.output';

describe('QueryWithdrawOutput', () => {
  it('should be defined', () => {
    expect(new QueryWithdrawOutput()).toBeDefined();
  });
});
