import { CheckBalanceOutput } from './check-balance.output';

describe('CheckBalanceOutput', () => {
  it('should be defined', () => {
    expect(new CheckBalanceOutput()).toBeDefined();
  });
});
