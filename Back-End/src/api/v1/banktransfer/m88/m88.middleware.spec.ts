import { M88Middleware } from './m88.middleware';

describe('M88Middleware', () => {
  it('should be defined', () => {
    // @ts-ignore
    expect(new M88Middleware()).toBeDefined();
  });
});
