import { ResendCallbackInput } from './resend-callback.input';

describe('ResendCallbackInput', () => {
  it('should be defined', () => {
    expect(new ResendCallbackInput()).toBeDefined();
  });
});
