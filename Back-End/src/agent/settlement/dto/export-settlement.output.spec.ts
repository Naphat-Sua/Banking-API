import { ExportSettlementOutput } from './export-settlement.output';

describe('ExportSettlementOutput', () => {
  it('should be defined', () => {
    expect(new ExportSettlementOutput()).toBeDefined();
  });
});
