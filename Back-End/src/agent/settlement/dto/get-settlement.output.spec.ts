import { GetSettlementOutput } from './get-settlement.output';

describe('GetSettlementOutput', () => {
  it('should be defined', () => {
    expect(new GetSettlementOutput()).toBeDefined();
  });
});
