import { EditProfileOutput } from './edit-profile.output';

describe('EditProfileOutput', () => {
  it('should be defined', () => {
    expect(new EditProfileOutput()).toBeDefined();
  });
});
