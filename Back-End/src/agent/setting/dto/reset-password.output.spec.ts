import { ResetPasswordOutput } from './reset-password.output';

describe('ResetPasswordOutput', () => {
  it('should be defined', () => {
    expect(new ResetPasswordOutput()).toBeDefined();
  });
});
