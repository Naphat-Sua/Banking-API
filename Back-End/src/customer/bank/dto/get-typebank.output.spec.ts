import { GetTypebankOutput } from './get-typebank.output';

describe('GetTypebankOutput', () => {
  it('should be defined', () => {
    expect(new GetTypebankOutput()).toBeDefined();
  });
});
