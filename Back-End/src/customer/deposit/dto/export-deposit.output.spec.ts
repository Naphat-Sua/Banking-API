import { ExportDepositOutput } from './export-deposit.output';

describe('ExportDepositOutput', () => {
  it('should be defined', () => {
    expect(new ExportDepositOutput()).toBeDefined();
  });
});
